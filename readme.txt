Command line tool which prompts user for two separate inputs (start date &e end date) and then returns the amount of days in between the two dates.

Format:

Start date? [month] [date]
End date?   [month] [date]
